import { Platform } from 'react-native';

// Production
 export const API_BASE_URL = "https://dosebox.in/api";

// Development (Uncomment to use local backend)
// const DEV_URL = Platform.OS === 'android' ? "http://192.168.1.6:3000/api" : "http://localhost:3000/api";
// export const API_BASE_URL = DEV_URL;
